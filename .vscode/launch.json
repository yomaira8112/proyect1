
 {
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: install:client",
            "request": "launch",
            "command": "npm run install:client",
            "cwd": "${workspaceFolder}"
        }
    ]
}